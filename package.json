{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Aman Sigroha portfolio — full stack on Vercel",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "node scripts/sync-resume.cjs && npm run build --prefix client",
    "sync-resume": "node scripts/sync-resume.cjs",
    "db:setup": "npm run db:setup --prefix server"
  }
}
