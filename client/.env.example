# Copy to .env.production.local for production builds
# Set this in Vercel project settings for production deploys

# Railway backend URL (no trailing slash)
VITE_API_URL=https://your-app.up.railway.app
